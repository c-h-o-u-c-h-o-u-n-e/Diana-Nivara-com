import React from 'react';
import CommonTitle from '../CommonTitle';

const RatesTitle = React.memo(() => {
  return <CommonTitle title="Rates" />;
});

RatesTitle.displayName = 'RatesTitle';

export default RatesTitle;
